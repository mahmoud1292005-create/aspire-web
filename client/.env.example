# Set this to your deployed Cloudflare Worker URL, e.g.
# https://aspire-server.<your-subdomain>.workers.dev/api
# Leave unset for local dev - it falls back to '/api', proxied to the
# local server by vite.config.js.
VITE_API_URL=
